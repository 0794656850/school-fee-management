# --- Flask / App ---
FLASK_ENV=production
SECRET_KEY=change-me
SESSION_COOKIE_SECURE=1
SESSION_COOKIE_SAMESITE=Lax
PREFERRED_URL_SCHEME=https

# --- Database ---
MYSQL_ROOT_PASSWORD=rootpass
MYSQL_DATABASE=school_fee_db
MYSQL_USER=app
MYSQL_PASSWORD=apppass
SQLALCHEMY_DATABASE_URI=mysql+pymysql://app:apppass@db/school_fee_db

# --- Branding (optional) ---
APP_NAME=Fee Management Portal
BRAND_NAME=Your School
PORTAL_TITLE=Portal
LOGO_PRIMARY=img/smartedupay_logo.svg
LOGO_SECONDARY=img/smartedupay_logo_secondary.svg
FAVICON=img/smartedupay_logo.svg

# --- Login defaults (app-level fallback) ---
APP_LOGIN_USERNAME=user
APP_LOGIN_PASSWORD=9133

# --- Twilio (optional) ---
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# --- WhatsApp Cloud API (optional) ---
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_TEMPLATE_NAME=
WHATSAPP_TEMPLATE_LANG=en_US

# --- M-Pesa Daraja (optional) ---
DARAJA_ENV=sandbox
DARAJA_CONSUMER_KEY=
DARAJA_CONSUMER_SECRET=
DARAJA_SHORT_CODE=
DARAJA_PASSKEY=
DARAJA_CALLBACK_URL=https://<your-ngrok-subdomain>.ngrok.io/mpesa/callback
DARAJA_ACCOUNT_REF=FMS-PRO-2025T3
DARAJA_TRANSACTION_DESC=Fee Mgmt Pro Upgrade (2025 T3)
PRO_PRICE_KES=1500

# --- Gmail API (optional) ---
# Path to your OAuth 2.0 client credentials JSON downloaded from Google Cloud
GMAIL_CREDENTIALS_JSON=credentials.json
# Where to store the user token after consent
GMAIL_TOKEN_JSON=token.json
# Redirect URI used during OAuth consent. For local dev, keep it HTTP and
# add the same URI in Google Cloud Console (OAuth client) Authorized redirect URIs.
# Examples:
#   http://127.0.0.1:5000/oauth2callback
#   http://localhost:5000/oauth2callback
GMAIL_REDIRECT_URI=
# For local HTTP development only: allow oauthlib to accept http redirect URIs
# NEVER enable this in production.
OAUTHLIB_INSECURE_TRANSPORT=1

# Billing / Licensing (for billing.py)
LICENSE_SECRET=
ADMIN_EMAIL=
BASE_URL=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=
